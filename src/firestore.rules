
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas abiertas para desarrollo según lo solicitado por el usuario
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
