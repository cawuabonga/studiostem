
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: Disallow all reads and writes by default.
    // This is the safest starting point. 
    // We will open up specific paths as needed.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
